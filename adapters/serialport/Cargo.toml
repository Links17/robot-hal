[package]
name = "seeed-hal-adapter-serialport"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[features]
hardware-loopback = []
hardware-tests = ["hardware-loopback"]

[dependencies]
async-trait = { workspace = true }
bytes = { workspace = true }
percent-encoding = { workspace = true }
seeed-hal-core = { path = "../../crates/seeed-hal-core", version = "=0.5.0-rc.1"}
seeed-hal-serial = { path = "../../crates/seeed-hal-serial", version = "=0.5.0-rc.1"}
serial2 = { workspace = true, features = ["unix"] }
serialport = { workspace = true }
tokio = { workspace = true }

[target.'cfg(unix)'.dependencies]
libc = { workspace = true }

[target.'cfg(windows)'.dependencies]
windows-sys = { workspace = true, features = [
    "Win32_Devices_Communication",
    "Win32_Foundation",
] }

[dev-dependencies]
seeed-hal-testkit = { path = "../../crates/seeed-hal-testkit", version = "=0.5.0-rc.1"}
