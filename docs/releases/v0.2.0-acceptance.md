# v0.2.0 acceptance evidence

**Date:** 2026-08-17 (Asia/Shanghai)  
**Implementation branch:** `feat/v0.2-can`  
**Host:** macOS arm64; Rust 1.85; Python 3.11

## Local hardware-free gate

| Check | Result |
| --- | --- |
| `cargo fmt --all --check` | Passed |
| `./scripts/check-generated-protocol.sh` | Passed |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | Passed |
| `cargo test --workspace --all-features` | Passed; physical Serial and CAN tests remained ignored by design |
| `uv run --project bindings/python --python 3.11 --frozen pytest -q` | Passed: 183 tests |
| runner contract tests | Passed: 9 tests |
| virtual broker black-box conformance | Passed: Serial and CAN checks |

The black-box executable was built with `virtual-adapters`, negotiated wire minor 1, and exercised
the v0.1 Serial path plus virtual CAN enumerate/open/close. All queues and process deadlines remain
bounded by the runner.

## Cross-target checks

`cargo clippy --target x86_64-pc-windows-msvc --workspace --all-targets --all-features -- -D warnings`
passed on this host.

The corresponding `x86_64-unknown-linux-gnu` check remains an environmental block: its `libudev-sys`
build requires a target sysroot and cross-aware `pkg-config`, neither of which is installed locally.
This is not a claim of native Linux execution. Native Linux SocketCAN `vcan`, PCAN-Basic runtime and
hardware loopback, native Windows CI execution, and physical Serial loopback remain external gates.
