# v0.1.0 acceptance evidence

**Qualification status:** Local macOS and hardware-free executable gates passed. Three-platform
release qualification is pending native Linux and Windows CI plus physical Serial acceptance.

**Evidence date:** 2026-08-15 (Asia/Shanghai)

**Host:** macOS, `aarch64-apple-darwin`

**Implementation commit under test:** `21c9173339c06b1bf763572bb02885333c14a904` plus the Task 9
qualification change set committed as `test(v0.1): qualify core and serial vertical slice`

**Toolchain:** `rustc 1.85.1`, `cargo 1.85.1`, `uv 0.8.22`, Python `3.11.13`

No push or remote CI run was performed. There are no CI URLs or remote results associated with this
evidence.

## Local results

| Gate | Exact command | Result |
| --- | --- | --- |
| Rust format | `cargo fmt --all --check` | PASSED, exit 0 |
| Rust lint | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | PASSED, exit 0 |
| Rust suite | `cargo test --workspace --all-features` | PASSED: 130 passed, 0 failed, 1 ignored physical test |
| Python 3.11 | `cd bindings/python && uv run --frozen pytest -q` | PASSED: 95 passed |
| Generated protobuf, run 1 | `./scripts/check-generated-protocol.sh` | PASSED, exit 0 |
| Generated protobuf, run 2 | `./scripts/check-generated-protocol.sh` | PASSED, exit 0 |
| Runner contract | `uv run --project bindings/python --frozen pytest -q tests/conformance/test_runner_contract.py` | PASSED: 3 passed |
| Broker build | `cargo build -p seeed-hal-broker-app --features virtual-adapter` | PASSED, exit 0 |
| Broker black-box | `uv run --project bindings/python --frozen python tests/conformance/run-broker-conformance.py --broker target/debug/seeed-hal-broker` | PASSED: 9 contract checks |
| Broker manifest | `target/debug/seeed-hal-broker --manifest` | PASSED: v0.1.0, wire 1.0, `aarch64-apple-darwin`, virtual adapter feature, SHA-256 present |
| Windows Rust cross-check | `RUSTFLAGS='-D warnings' cargo check --workspace --all-targets --all-features --target x86_64-pc-windows-msvc` | PASSED, exit 0 |
| Linux Rust cross-check | `RUSTFLAGS='-D warnings' cargo check --workspace --all-targets --all-features --target x86_64-unknown-linux-gnu` | BLOCKED before workspace completion by missing Linux libudev pkg-config sysroot/cross wrapper |

The Linux blocker is from `libudev-sys v0.1.4`: `pkg-config has not been configured to support
cross-compilation`; it requests a target sysroot with `PKG_CONFIG_SYSROOT_DIR`/`PKG_CONFIG_PATH` or a
cross-compiling `PKG_CONFIG` wrapper. The host was not broadly mutated to manufacture that sysroot.

## Black-box coverage

The runner launched the built broker executable, used a compile-time-only virtual adapter, and
exchanged real big-endian length-prefixed protobuf frames. Bounded checks covered authentication and
capability handshake, enumeration, open, write/read loopback, flush, control lines, fencing generation
advance and stale-generation rejection, abrupt owner disconnect cleanup, resource reuse by a new
owner, and graceful broker shutdown. Temporary token, endpoint, process, and directory cleanup are
owned by the runner. No physical device or device serial number was used.

## Platform and external gates

| Platform/gate | Status | Evidence still required |
| --- | --- | --- |
| macOS arm64 local Rust/Python/black-box | PASSED | None for this host-local gate |
| Windows Rust compile, warnings denied | PASSED | Native Windows CI tests, Python Named Pipe black-box execution, and broker manifest |
| Linux Rust cross-compile | BLOCKED | Configured libudev target sysroot, then native Linux CI tests and Unix socket black-box execution |
| GitHub Actions matrix | PENDING | Linux, macOS, and Windows jobs must run the committed workflow and pass; no remote run was triggered |
| Physical Serial loopback | PENDING | Chipset/OS/endpoint/baud/payload/unplug/cleanup evidence from the runbook; device serial must not be persisted |

The default all-features Rust suite confirmed the hardware loopback test remains ignored without an
explicit ignored-test invocation. CAN/CAN FD, USB, GPIO, and Camera remain planned and are not part of
this acceptance claim. The release is not fully qualified until the pending native and physical gates
are completed.
